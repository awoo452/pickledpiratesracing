<%= form_with model: @product,
              url: admin_product_path(@product),
              method: :patch,
              local: true,
              html: {
                multipart: true,
                data: { turbo: false }
              } do |f| %>

  <%= f.file_field :image %>
  <%= f.submit "Upload" %>

<% end %>
